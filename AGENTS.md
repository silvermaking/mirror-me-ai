# Mirror Me AI 협업 규칙

## 모든 세션의 시작과 자율 재개

- `AGENTS.md`는 변하지 않는 협업·품질 규칙이고, `docs/PROJECT_STATUS.md`는 현재 체크포인트와 다음 작업의 유일한 진행 원장이다. 새 세션은 반드시 `AGENTS.md → docs/PROJECT_STATUS.md → git status --short --branch → git log -5` 순서로 확인한다.
- Session 1과 Session 2는 특정 채팅의 이름이 아니라 **역할**이다. 현재 브랜치·worktree·소유 파일로 역할을 판별하고, 역할이 불명확하면 구현을 시작하지 말고 먼저 `docs/PROJECT_STATUS.md`의 활성 작업을 정리한다.
- 사용자가 `어디까지 했어`, `현황만`, `뭐가 남았어`처럼 상태만 물으면 `현재 / NEXT / 막힘`만 네 줄 이내로 답하고 파일이나 Goal을 바꾸지 않는다.
- 사용자가 `다음 작업 진행`, `이어가`, `진행해`, `이제 다음 거 해`처럼 실행을 요청하면 추가 프롬프트를 요구하지 않는다. 진행 원장의 기존 `ACTIVE`를 재개하고, 없으면 `WAITING_REVIEW`, 그다음 정확히 하나의 `NEXT/READY` 순으로 맡는다.
- 실행 항목이 두 개 이상의 체크포인트·여러 자동 계속 실행·하위 에이전트 협업·두 개 이상의 커밋을 요구하면 Goal을 만든다. 그보다 작은 문서 수정, 단일 검수와 한 번의 테스트는 Goal 없이 끝낸다.
- Goal 이름은 `<WORK-ID>: <구체적 결과>; 완료 조건: <테스트·캡처·SHA>` 형식으로 만든다. `진행`, `품질 향상`, `게임 완성` 같은 무제한 Goal, 아트·오디오·HUD를 묶은 Goal, 완료 뒤 다음 단계까지 자동 확장하는 Goal은 금지한다.
- `NEXT`는 항상 하나만 둔다. 파일 소유권이 겹치지 않는 병렬 항목만 별도 `READY`로 둘 수 있으며, 같은 항목이나 같은 파일을 다른 세션이 이미 맡았으면 검수 역할로 전환한다.
- 시작 전 dirty worktree는 사용자 작업으로 간주해 보존한다. 진행 원장과 Git이 다르면 Git을 증거로 삼되, Session 1 역할이 원장을 먼저 교정한다.
- 각 체크포인트는 테스트·캡처·커밋 SHA와 정확한 다음 행동을 원장에 남긴다. 중단되거나 Goal이 삭제되어도 새 세션이 원장만으로 이어갈 수 있어야 한다.
- 사용자 확인은 dirty 변경 충돌, 서로 다른 방향 계약, 외부 권한, 공동 디렉터의 취향 결정처럼 안전하게 추론할 수 없는 경우에만 요청한다. 그 외에는 기록된 `NEXT`를 자율적으로 진행한다.

## 현재 단계

- 시간에 따라 변하는 브랜치·완료 항목·활성 작업·`NEXT`는 이 절에 덧붙이지 않고 `docs/PROJECT_STATUS.md`에서만 갱신한다.
- 3D Kiln Reliquary 런타임은 `codex/archive-3d-runtime` @ `630e0da`에 비교 기준으로 보존한다. 삭제하거나 같은 브랜치에서 2D로 덮어쓰지 않는다.
- 현재 제작 대상은 공동 디렉터가 요청한 **최초판 게임필 + 핸드페인티드 2D 스프라이트 challenger**다. 최상위 아트 계약은 `docs/design/SPRITE_REBUILD_DIRECTION.md`를 따르고, `docs/design/CLASSIC_ASSET_CHALLENGER.md`는 게임필·좌표·fallback 비교 기준으로 보존한다.
- 최초 완성판 `e63f7a0`의 연속 Canvas 움직임과 게임필을 절대 비교 기준으로 삼고, 현재 지도 2D판은 `d745fa4`에 비교 기준으로 보존한다.
- `src/game-core.mjs`의 2D 판정 평면을 유일한 게임 진실로 보존하며 기존 core tests를 새 외형에 맞춰 바꾸지 않는다.
- 기능을 추가하지 않고 최초판의 연속 위험 예고·대시 흔적·검 접촉·LOCK 좌표는 보존하되, 조립식 SVG 캐릭터는 동작마다 큰 외곽선이 달라지는 전신 키포즈 스프라이트로 교체한다.
- 검토용 생성 이미지는 대화 또는 `work/`에서만 다루며 런타임 배경·스프라이트나 커밋 자산으로 사용하지 않는다.
- Session 1은 이미지를 직접 생성하지 않고 기획·작업 지시·독립 검수·최종 채택을 맡는다. 이미지와 스프라이트 시안은 디자인 subagent 또는 별도 디자인 세션이 제작한다.
- Session 2는 방향 잠금 커밋을 반영한 뒤 게임 코드, HTML, CSS, 테스트와 GitHub Pages 배포 구성을 수정할 수 있다.
- 별도 서버나 설치 과정 없이 정적 웹 페이지에서 바로 실행되어야 한다.
- 기능을 늘리지 않고 확정된 이동·공격·대시·학습·LOCK·코어 타격·재도전 루프만 구현한다.
- 저장소의 상대 경로로 모든 자산을 불러와 GitHub Pages의 하위 경로에서도 동작하게 한다.

## 확정된 크리에이티브 방향

- 플레이어가 캐릭터를 실시간으로 직접 이동·공격·대시해 AI 보스를 쓰러뜨리는 액션 게임이다.
- 화면은 최초판과 같은 고정 쿼터뷰 Canvas 2D이며, 이동과 피격의 진실은 하나의 2D 바닥 평면에만 존재한다.
- 스프라이트 높이, 무기 궤적, 파편과 그림자는 시각 표현일 뿐 판정에 관여하지 않는다.
- 자유 카메라, 점프, 높이 판정, 수직 조준, 플랫폼 이동은 현재 범위가 아니다.
- 물리적인 좌우 탈출문과 선택 대기 구조는 폐기한다.
- AI가 처음 학습하는 행동은 탐색 공격 종료 순간 위험 구역 밖에 있는 실제 회피 측면 하나뿐이다. WASD와 대시는 같은 종료 위치라면 같은 표본이다.
- AI의 오판은 자동 피해가 아니라 코어 노출을 만든다. 보스 체력은 플레이어가 직접 공격했을 때만 줄어든다.
- 새 세계관 설명보다 최초판에서 이미 읽히던 회피·기억·LOCK·헛공격·직접 타격의 물리적 인과를 우선한다.

## 비주얼 재설계 원칙

- 검정 배경, 청록·자홍 네온, 대칭 HUD 상자, 다면체 로봇을 자동으로 미래적이라고 간주하지 않는다.
- 팔레트 교체만으로 고도화했다고 판단하지 않는다. 실루엣, 재질, 공간, 애니메이션의 예비동작·충돌·반동이 함께 바뀌어야 한다.
- 정지 화면에서는 플레이어, 보스, 현재 위험, 예측 위치, 열린 코어가 문구 없이 구분되어야 한다.
- 무성 8초 영상에서는 `회피 → 기억 → LOCK → 헛공격 → 코어 노출 → 직접 타격`의 인과가 읽혀야 한다.
- UI를 숨겨도 실제 장소에서 두 개체가 충돌하는 장면처럼 보여야 한다. 전장을 빈 배경이나 HUD 받침대로 다루지 않는다.
- 생성 이미지는 구도와 미술 방향을 비교하는 자료다. 생성 이미지의 임의 HUD, 글자, 파츠 수, 체력 변화, 카메라 변화는 게임 규칙으로 채택하지 않는다.
- 고유성을 설명할 때 다른 게임 이름이나 장르 수식어만 나열하지 말고, 이 게임만의 물리적 기억 장치와 오판으로 무너지는 보스 자세를 제시한다.
- 플레이어는 짙은 코발트 망토·밝은 얼굴·분리되는 검, 보스는 먹빛 몸통·상아 외피·과장된 한쪽 파일드라이버의 비대칭 실루엣으로 구분한다.
- 기억은 정확히 세 개, LOCK은 움직이지 않는 주홍 인장, 코어는 화면에서 가장 밝은 순백색 면이다.
- authored 2D의 품질은 큰 명암 덩어리, 비대칭 실루엣, 접촉 전 예비동작과 충돌 후 연쇄 반동으로 판단한다. 생성 대표 장면을 잘라 게임 자산으로 쓰거나 한 장의 고정 포즈로 연속 움직임을 대체하면 실패다.
- ImageGen은 마스터 디자인과 키포즈 탐색에 사용한다. 런타임 후보는 별도의 동작별 원본으로 제작하고 정체성·시점·팔레트·윤곽·anchor를 정리한 뒤 결정적 strip build를 통과해야 한다.
- Blender, GLB와 하나의 통짜 atlas는 새 challenger의 제작 또는 런타임 의존성이 아니다. 동작별 투명 PNG strip과 JSON anchor manifest는 허용한다.

## 공동 디렉팅 원칙

- 사용자와 루트 에이전트를 공동 총괄 디렉터로 본다.
- 하위 에이전트는 독립안을 제시하되 최종 방향을 임의로 확정하지 않는다.
- 모든 제안은 다음 세 장면 중 무엇을 개선하는지 명시한다.
  1. 시작 후 10초 안의 첫 훅
  2. AI가 나를 학습했다는 것이 보이는 순간
  3. 게임 오버 직후 다시 도전하고 싶어지는 순간
- 추상어만 쓰지 말고 화면 구성, 보스 행동, 플레이어 입력, 짧은 문구, 소리와 시간 흐름까지 실제 플레이 장면으로 설명한다.
- 새 규칙을 제안하려면 기존 규칙 하나를 대체하거나 제거할 수 있는지 먼저 검토한다.
- 공동 디렉터에게 올리는 검토안은 방향별 **한 문장과 대표 이미지 한 장**으로 제한한다. 긴 근거와 에이전트 토론은 부록으로 분리한다.
- 확인 요청은 `권고 / 이유 / 답변 형식` 세 줄 이내로 작성하고, 답을 기다리는 동안 멈출 파일을 함께 밝힌다.

## 두 세션 운영

- 두 세션은 같은 작업을 나누지 않는다. **Session 1은 총괄 디렉팅·통합**, **Session 2는 플레이어블 구현**을 소유한다.
- Session 1은 `docs/PROJECT_STATUS.md`에 기록된 통합 후보 브랜치에서 기획 잠금, 구현 리뷰와 최종 병합만 담당한다. 로컬 `main`이 최신이라고 가정하지 않으며 게임 구현 파일을 직접 고치지 않는다.
- Session 2는 진행 원장의 기준 SHA에서 만든 별도 Git worktree와 항목별 `codex/<work-id>-<slug>` 브랜치에서만 작업한다. 같은 파일을 두 세션이 동시에 수정하지 않는다.
- Session 1은 `gpt-5.6-sol / ultra`, Session 2는 `gpt-5.6-terra / high`를 기본으로 한다. 구현이 반복해서 막히는 경우에만 Session 1이 Sol 검토를 수행한다.
- 각 세션의 동시 하위 에이전트는 최대 2명으로 제한한다. 한 명은 제안, 한 명은 PASS/FAIL 검증을 맡고 같은 질문을 중복 조사하지 않는다.
- 하위 에이전트의 디렉터 전달물은 `결론 한 문장 + 근거 최대 다섯 줄`로 제한한다.
- Session 1은 `docs/`, `work/reviews/`, 콘셉트 자료를 소유하고 구현 파일에는 리뷰만 남긴다.
- Session 2는 `src/`, `tests/`, `index.html`, `styles.css`, `assets/2d/`와 배포 구성을 소유한다. 기존 `assets/3d/`와 Blender 원본은 보존하며 기획 문서를 임의로 바꾸지 않는다.
- `docs/PROJECT_STATUS.md`의 최종 상태 변경은 Session 1이 소유한다. Session 2는 자기 브랜치의 체크포인트를 보고하고, Session 1이 실제 검증 뒤 `DONE`, `FIX_REQUIRED` 또는 다음 `READY`를 기록한다.
- Session 2는 `DIRECTION LOCKED @ <commit SHA>`를 받은 뒤 구현을 시작한다.
- Session 2는 `PLAN READY`, `FIRST PLAYABLE`, `REVIEW READY @ <commit SHA>`, `NEEDS DECISION` 체크포인트로 보고한다.
- Session 1은 다른 작업을 읽고 기다리며 직접 후속 메시지를 보낸다. 공동 디렉터가 두 작업 사이에서 내용을 복사하지 않게 한다.
- 공용 규칙 변경은 소유 세션이 커밋하고 상대 세션이 그 SHA를 반영한 뒤 계속한다.
- 세부 절차와 핸드오프 형식은 `docs/TWO_SESSION_WORKFLOW.md`를 따른다.

## 역할별 규칙

### 게임 기획

- 핵심 선택과 위험·보상의 관계를 검토한다.
- 무작위 플레이, 정답 반복, 시간 끌기처럼 핵심 재미를 무력화하는 전략을 먼저 찾는다.
- 첫 플레이어가 설명 없이 무엇을 시도할지 장면 단위로 검증한다.

### UX·비주얼 디자인

- 정보는 보스 본체와 전장 안에서 읽히게 하고 HUD 문구에만 의존하지 않는다.
- 시각 자료가 판단을 돕는 경우 이미지 스토리보드를 사용한다.
- sprite challenger 장면과 최소 DOM UI는 `docs/design/SPRITE_REBUILD_DIRECTION.md`의 카메라·색·형태·모션 문법을 따른다. 기존 SVG·지도 2D와 3D 문서는 보관된 비교 기준이다.
- 화면 크기가 달라도 전체 원형 전장, 플레이어, 보스, 현재 공격 예고가 함께 보여야 한다.
- 생성 이미지는 최종 아트가 아니라 화면 우선순위와 순간 연출을 합의하기 위한 자료로 취급한다.

### 개발 타당성 검토

- 게임 상태와 렌더링·입력을 분리해 핵심 판정을 자동 검증할 수 있게 한다.
- 외부 서비스, 계정, 네트워크 요청과 필수 런타임 의존성을 추가하지 않는다.
- Session 2는 `PLAN READY`에서 Canvas 2D 렌더러, 동작별 PNG strip, JSON anchor manifest와 결정적 build 방식을 제안한다. 새 대형 엔진이나 런타임 CDN을 추가하지 않고 기존 정적 페이지 구조를 우선 재사용한다.
- 실제 스프라이트는 동작별 무손실 master frame, 팔레트, named anchor manifest와 런타임 strip을 함께 보존하고 저장소 상대 경로로만 불러온다. 최소 anchor는 player `feet/sword_grip/sword_tip`, boss `root/driver_joint/driver_tip/core_center/brace_contact/memory_slot_1..3`이며 LOCK·직접 타격·접지 오차를 데스크톱과 320×180에서 1px 이내로 검증한다. 생성 검토 이미지는 런타임 자산이 아니다.
- 보관된 Blender 원본·GLB·Three.js vendor를 삭제하지 않는다. 2D challenger가 선택되기 전에는 3D 배포를 파괴하지 않는다.
- 기존 판정 테스트를 2D 구현에 맞춰 수정하지 않는다. `game-core.mjs`가 변경되면 원칙 위반으로 간주한다.
- 플레이어가 관찰할 수 있는 입력과 게임이 판정하는 결과 사이에 모호함이 없는지 검토한다.
- 예측이 잠긴 뒤 바뀌지 않는지, 강제된 행동이 학습되지 않는지, 실패 원인이 재현 가능한지 확인한다.
- 복잡도를 줄일 수 있는 경우 반드시 더 단순한 대안을 함께 제시한다.

## 교차 검증 기준

- 10초 훅은 플레이어가 실제로 한 번 입력하고, AI의 반응을 보고, 성공 또는 실패의 결과까지 경험해야 한다.
- 학습 순간은 이전 행동의 흔적, 현재 예측, 그 예측을 실행하는 공격이 하나의 인과로 보여야 한다.
- 재도전 이유는 보상 약속보다 “내가 다음 판에 바꿀 한 가지”가 명확해야 한다.
- 보스가 영리해 보이기 위해 공정성을 희생하지 않는다.
- 문서에 남기는 규칙은 한 문장으로 설명할 수 있고 실제 화면 장면으로 그릴 수 있어야 한다.

## 산출물 규칙

- 최종 기획 결정은 `docs/GAME_DESIGN.md`에 반영한다.
- 검토용 메모와 시각 자료는 `work/`에서 관리한다.
- 플레이 가능한 코드, 테스트, 조작 안내와 배포 구성은 저장소에 남긴다.
- 범위를 벗어난 임시 기능, 생성된 캐시, 로컬 전용 설정은 커밋하지 않는다.
