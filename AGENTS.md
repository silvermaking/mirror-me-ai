# Mirror Me AI 협업 규칙

## 현재 단계

- 현재는 플레이 가능한 판정 코어를 보존한 채, 제네릭 네온 프로토타입을 고유한 액션게임 비주얼과 전투 손맛으로 재설계하는 단계다.
- 동일한 `OUTSMART` 장면의 미술 방향 세 가지 비교는 끝났으며, 현재 **A. Kiln Reliquary**를 우선안으로 두고 역동적 전투 키프레임을 최종 확인한다.
- A는 우선안일 뿐 아직 확정안이 아니다. 공동 디렉터가 `A 확정`이라고 답하기 전까지 `DIRECTION LOCKED`로 취급하지 않는다.
- 공동 디렉터가 미술 방향을 선택하기 전에는 게임 기능 코드와 최종 렌더링을 변경하지 않는다.
- 검토용 이미지와 HTML은 `work/` 또는 대화용 시각화 경로에 두고, 선택되지 않은 생성 이미지를 게임 자산처럼 사용하지 않는다.
- 방향 확정 뒤에는 게임 코드, HTML, CSS, 테스트와 GitHub Pages 배포 구성을 수정할 수 있다.
- 별도 서버나 설치 과정 없이 정적 웹 페이지에서 바로 실행되어야 한다.
- 기능을 늘리지 않고 확정된 이동·공격·대시·학습·LOCK·코어 타격·재도전 루프만 구현한다.
- 저장소의 상대 경로로 모든 자산을 불러와 GitHub Pages의 하위 경로에서도 동작하게 한다.

## 확정된 크리에이티브 방향

- 플레이어가 캐릭터를 실시간으로 직접 이동·공격·대시해 AI 보스를 쓰러뜨리는 액션 게임이다.
- 화면은 고정 쿼터뷰 2.5D를 기준으로 하며, 이동과 피격의 진실은 하나의 2D 바닥 평면에만 존재한다.
- 2D 아트 대안도 같은 카메라, 실루엣, 전투 평면을 유지한다. 별도의 게임 방향으로 취급하지 않는다.
- 자유 카메라, 점프, 높이 판정, 수직 조준, 플랫폼 이동은 현재 범위가 아니다.
- 물리적인 좌우 탈출문과 선택 대기 구조는 폐기한다.
- AI가 처음 학습하는 행동은 탐색 공격 종료 순간 위험 구역 밖에 있는 실제 회피 측면 하나뿐이다. WASD와 대시는 같은 종료 위치라면 같은 표본이다.
- AI의 오판은 자동 피해가 아니라 코어 노출을 만든다. 보스 체력은 플레이어가 직접 공격했을 때만 줄어든다.

## 비주얼 재설계 원칙

- 검정 배경, 청록·자홍 네온, 대칭 HUD 상자, 다면체 로봇을 자동으로 미래적이라고 간주하지 않는다.
- 팔레트 교체만으로 고도화했다고 판단하지 않는다. 실루엣, 재질, 공간, 애니메이션의 예비동작·충돌·반동이 함께 바뀌어야 한다.
- 정지 화면에서는 플레이어, 보스, 현재 위험, 예측 위치, 열린 코어가 문구 없이 구분되어야 한다.
- 무성 8초 영상에서는 `회피 → 기억 → LOCK → 헛공격 → 코어 노출 → 직접 타격`의 인과가 읽혀야 한다.
- UI를 숨겨도 실제 장소에서 두 개체가 충돌하는 장면처럼 보여야 한다. 전장을 빈 배경이나 HUD 받침대로 다루지 않는다.
- 생성 이미지는 구도와 미술 방향을 비교하는 자료다. 생성 이미지의 임의 HUD, 글자, 파츠 수, 체력 변화, 카메라 변화는 게임 규칙으로 채택하지 않는다.
- 고유성을 설명할 때 다른 게임 이름이나 장르 수식어만 나열하지 말고, 이 게임만의 물리적 기억 장치와 오판으로 무너지는 보스 자세를 제시한다.

## 공동 디렉팅 원칙

- 사용자와 루트 에이전트를 공동 총괄 디렉터로 본다.
- 하위 에이전트는 독립안을 제시하되 최종 방향을 임의로 확정하지 않는다.
- 모든 제안은 다음 세 장면 중 무엇을 개선하는지 명시한다.
  1. 시작 후 10초 안의 첫 훅
  2. AI가 나를 학습했다는 것이 보이는 순간
  3. 게임 오버 직후 다시 도전하고 싶어지는 순간
- 추상어만 쓰지 말고 화면 구성, 보스 행동, 플레이어 입력, 짧은 문구, 소리와 시간 흐름까지 실제 플레이 장면으로 설명한다.
- 새 규칙을 제안하려면 기존 규칙 하나를 대체하거나 제거할 수 있는지 먼저 검토한다.
- 공동 디렉터에게 올리는 검토안은 방향별 **한 문장과 대표 이미지 한 장**으로 제한한다. 긴 근거와 에이전트 토론은 부록으로 분리한다.
- 확인 요청은 `권고 / 이유 / 답변 형식` 세 줄 이내로 작성하고, 답을 기다리는 동안 멈출 파일을 함께 밝힌다.

## 두 세션 운영

- 두 세션은 같은 작업을 나누지 않는다. **Session 1은 디자인 결정**, **Session 2는 플레이어블 구현**을 소유한다.
- 두 세션은 별도 브랜치와 별도 Git worktree를 사용하며 같은 파일을 동시에 수정하지 않는다.
- 각 세션의 동시 하위 에이전트는 최대 2명으로 제한한다. 한 명은 제안, 한 명은 PASS/FAIL 검증을 맡고 같은 질문을 중복 조사하지 않는다.
- 하위 에이전트의 디렉터 전달물은 `결론 한 문장 + 근거 최대 다섯 줄`로 제한한다.
- Session 1은 `docs/`, `work/reviews/`, 콘셉트 자료를 소유하고 구현 파일에는 리뷰만 남긴다.
- Session 2는 `src/`, `tests/`, `index.html`, `styles.css`와 배포 구성을 소유하고 기획 문서를 임의로 바꾸지 않는다.
- Session 2는 `DIRECTION LOCKED @ <commit SHA>`를 받기 전까지 구현 영역을 읽기 전용으로 둔다.
- 공용 규칙 변경은 소유 세션이 커밋하고 상대 세션이 그 SHA를 반영한 뒤 계속한다.
- 세부 절차와 핸드오프 형식은 `docs/TWO_SESSION_WORKFLOW.md`를 따른다.

## 역할별 규칙

### 게임 기획

- 핵심 선택과 위험·보상의 관계를 검토한다.
- 무작위 플레이, 정답 반복, 시간 끌기처럼 핵심 재미를 무력화하는 전략을 먼저 찾는다.
- 첫 플레이어가 설명 없이 무엇을 시도할지 장면 단위로 검증한다.

### UX·비주얼 디자인

- 정보는 보스 본체와 전장 안에서 읽히게 하고 HUD 문구에만 의존하지 않는다.
- 시각 자료가 판단을 돕는 경우 이미지 스토리보드를 사용한다.
- HTML, CSS와 Canvas 표현은 `docs/design/VISUAL_DIRECTION.md`의 카메라·색·형태 문법을 따른다.
- 화면 크기가 달라도 전체 원형 전장, 플레이어, 보스, 현재 공격 예고가 함께 보여야 한다.
- 생성 이미지는 최종 아트가 아니라 화면 우선순위와 순간 연출을 합의하기 위한 자료로 취급한다.

### 개발 타당성 검토

- 게임 상태와 렌더링·입력을 분리해 핵심 판정을 자동 검증할 수 있게 한다.
- 외부 서비스, 계정, 네트워크 요청과 필수 런타임 의존성을 추가하지 않는다.
- 플레이어가 관찰할 수 있는 입력과 게임이 판정하는 결과 사이에 모호함이 없는지 검토한다.
- 예측이 잠긴 뒤 바뀌지 않는지, 강제된 행동이 학습되지 않는지, 실패 원인이 재현 가능한지 확인한다.
- 복잡도를 줄일 수 있는 경우 반드시 더 단순한 대안을 함께 제시한다.

## 교차 검증 기준

- 10초 훅은 플레이어가 실제로 한 번 입력하고, AI의 반응을 보고, 성공 또는 실패의 결과까지 경험해야 한다.
- 학습 순간은 이전 행동의 흔적, 현재 예측, 그 예측을 실행하는 공격이 하나의 인과로 보여야 한다.
- 재도전 이유는 보상 약속보다 “내가 다음 판에 바꿀 한 가지”가 명확해야 한다.
- 보스가 영리해 보이기 위해 공정성을 희생하지 않는다.
- 문서에 남기는 규칙은 한 문장으로 설명할 수 있고 실제 화면 장면으로 그릴 수 있어야 한다.

## 산출물 규칙

- 최종 기획 결정은 `docs/GAME_DESIGN.md`에 반영한다.
- 검토용 메모와 시각 자료는 `work/`에서 관리한다.
- 플레이 가능한 코드, 테스트, 조작 안내와 배포 구성은 저장소에 남긴다.
- 범위를 벗어난 임시 기능, 생성된 캐시, 로컬 전용 설정은 커밋하지 않는다.
